{
  "name": "@microsoft/chartifact-schema-folder",
  "version": "1.0.0",
  "main": "index.js",
  "types": "dist/folder.d.ts",
  "type": "module",
  "scripts": {
    "clean": "rimraf dist",
    "schema": "ts-json-schema-generator --path src/schema.ts --type FolderWithSchema --out dist/folder.schema.json",
    "copy-schema": "node scripts/copy-schema.js",
    "declaration": "rollup -c rollup.config.types.js",
    "dev": "npm run esbuild:watch",
    "esbuild": "node esbuild.config.js",
    "esbuild:watch": "node esbuild.config.js --watch",
    "build": "npm run esbuild && npm run schema && npm run copy-schema",
    "build:00": "npm run build"
  },
  "author": "Dan Marshall",
  "license": "MIT",
  "description": ""
}
